{
  "name": "withus-extension",
  "version": "1.0.0",
  "description": "WITHUS Browser Extension — securely autofill credentials for delegated sessions",
  "scripts": {
    "build": "node build.mjs && node zip.js",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.5",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "adm-zip": "^0.6.0"
  }
}
