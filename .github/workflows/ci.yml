name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-lint:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Generate Prisma Client
      run: npm run generate --workspace=@repo/db

    - name: Lint
      run: npm run lint

    - name: Build API
      run: npm run build --workspace=api

    - name: Build Web
      run: npm run build --workspace=web

    # TODO: Future Production Infrastructure (Deferred)
    # - Docker build validation
    # - Automated testing
    # - Security scanning
    # - Image publishing
    # - Deployment
    # - Release automation
